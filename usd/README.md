# G1 asset and motion provenance

`g1_29dof_rev_1_0.usd`, its URDF, `instanceable_meshes.usd`, the mesh files in this directory, and the adjacent
`motions/G1_walk.npz`, `motions/G1_dance.npz`, and the retargeted `motions/G1_jump.npz` are adapted from
[`linden713/humanoid_amp`](https://github.com/linden713/humanoid_amp), commit
`99efa6d60e351ec3def0dc6e983baf143fd31328` (2025-10-18).

The source repository declares the BSD 3-Clause license. Its exact license text is preserved in
`LICENSE.humanoid_amp`. JOSE keeps the original attribution and uses the resources only for the 29-DOF Unitree G1
AMP example.
